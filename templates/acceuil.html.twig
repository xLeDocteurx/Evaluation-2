{% extends 'base.html.twig' %}
{% block title %}Acceuil{% endblock %}

{% block body %}

<h1 class="center-align">Acceuil</h1>

<div class="row">
    <div class="col s10 offset-s1">
        <div class="card">
            <div class="card-content">
                <p>
                    Endroit en toute convivialité pour donner ce que vous n'utilisez plus !
                </p>

                {% if app.user %}
                {% else %}
                {% endif %}
            </div>
        </div>
    </div>
</div>

{% endblock %}